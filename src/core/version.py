"""The platform version.

Kept here rather than at the source root because the layout is flat — there is no
package ``__init__`` above everything else to hold it.
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
